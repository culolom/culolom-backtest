          
name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/15 * * * *"   # 每 15 分鐘 ping 一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit app
        run: |
          curl -L --max-time 30 https://hamrlab.streamlit.app
