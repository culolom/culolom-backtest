name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/60 * * * *"   # 每 15 分鐘一次
  workflow_dispatch:        # 可以手動跑

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -L https://hamrlab.streamlit.app
