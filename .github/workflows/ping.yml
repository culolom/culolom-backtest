name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit app (ignore errors)
        run: |
          curl -L --max-time 30 https://your-app-name.streamlit.app || true
