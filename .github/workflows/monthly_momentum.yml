name: Monthly Momentum Update

on:
  schedule:
    # ⚠️ 設定在每月 1 號 09:00 (UTC 01:00)
    # 這是為了確保您的 "Auto update CSV data" (通常在 00:00 跑) 已經跑完並更新了 CSV
    - cron: '0 1 1 * *'
  
  workflow_dispatch: # 允許手動執行

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install pandas
        # 不需要 yfinance，因為我們讀本地 CSV

      - name: Run calculation script
        # 👇 關鍵：指向 scripts 資料夾下的檔案
        run: python scripts/update_momentum.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add momentum.json
          git commit -m "Auto-update momentum.json 🚀" || echo "No changes to commit"
          git push
